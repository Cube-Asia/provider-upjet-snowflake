apiVersion: preview.snowflake.m.crossplane.io/v1alpha1
kind: FunctionScala
metadata:
  annotations:
    meta.upbound.io/example-id: preview/v1alpha1/functionscala
  labels:
    testing.upbound.io/example-name: minimal
  name: minimal
  namespace: crossplane-system
spec:
  forProvider:
    arguments:
    - argDataType: VARCHAR(100)
      argName: x
    database: ${snowflake_database.test.name}
    functionDefinition: |2
        class TestFunc {
          def echoVarchar(x : String): String = {
            return x
          }
        }
    handler: TestFunc.echoVarchar
    name: my_scala_function
    returnType: VARCHAR(100)
    runtimeVersion: "2.12"
    schema: ${snowflake_schema.test.name}
