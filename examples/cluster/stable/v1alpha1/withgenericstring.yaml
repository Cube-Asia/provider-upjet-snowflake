apiVersion: stable.snowflake.crossplane.io/v1alpha1
kind: WithGenericString
metadata:
  annotations:
    meta.upbound.io/example-id: stable/v1alpha1/withgenericstring
  labels:
    testing.upbound.io/example-name: test
  name: test
spec:
  forProvider:
    database: EXAMPLE_DB
    schema: EXAMPLE_SCHEMA
    secretStringSecretRef:
      key: example-key
      name: example-secret
      namespace: upbound-system
