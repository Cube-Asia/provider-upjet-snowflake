---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: icebergtablefromfiles.preview.snowflake.crossplane.io
spec:
  group: preview.snowflake.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - snowflake
    kind: IcebergTableFromFiles
    listKind: IcebergTableFromFilesList
    plural: icebergtablefromfiles
    singular: icebergtablefromfiles
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IcebergTableFromFiles is the Schema for the IcebergTableFromFiless
          API. Resource used to manage an Iceberg table whose metadata is created
          from an existing Apache Iceberg metadata file in an external volume. For
          more information, check the official documentation https://docs.snowflake.com/en/sql-reference/sql/create-iceberg-table-iceberg-files.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IcebergTableFromFilesSpec defines the desired state of IcebergTableFromFiles
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  catalog:
                    description: |-
                      level default is used.
                      Specifies the identifier for the catalog integration to use for the Iceberg table. If not specified, the account-level default is used.
                    type: string
                  comment:
                    description: |-
                      (String) Specifies a comment for the Iceberg table.
                      Specifies a comment for the Iceberg table.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the Iceberg table. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the Iceberg table. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  externalVolume:
                    description: |-
                      level default is used.
                      Specifies the identifier for the external volume where the Iceberg table stores its metadata files and data in Parquet format. If not specified, the account-level default is used.
                    type: string
                  metadataFilePath:
                    description: Specifies the relative path of the Iceberg metadata
                      file in the external volume. Cannot be changed after creation.
                      External changes for this field won't be detected.
                    type: string
                  replaceInvalidCharacters:
                    description: |-
                      8 characters with the Unicode replacement character (�) in query results for an Iceberg table. For more information, check REPLACE_INVALID_CHARACTERS docs.
                      Specifies whether to replace invalid UTF-8 characters with the Unicode replacement character (`�`) in query results for an Iceberg table. For more information, check [REPLACE_INVALID_CHARACTERS docs](https://docs.snowflake.com/en/sql-reference/parameters#replace-invalid-characters).
                    type: boolean
                  schema:
                    description: |-
                      (String) The schema in which to create the Iceberg table. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the Iceberg table. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                required:
                - database
                - schema
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  catalog:
                    description: |-
                      level default is used.
                      Specifies the identifier for the catalog integration to use for the Iceberg table. If not specified, the account-level default is used.
                    type: string
                  comment:
                    description: |-
                      (String) Specifies a comment for the Iceberg table.
                      Specifies a comment for the Iceberg table.
                    type: string
                  externalVolume:
                    description: |-
                      level default is used.
                      Specifies the identifier for the external volume where the Iceberg table stores its metadata files and data in Parquet format. If not specified, the account-level default is used.
                    type: string
                  metadataFilePath:
                    description: Specifies the relative path of the Iceberg metadata
                      file in the external volume. Cannot be changed after creation.
                      External changes for this field won't be detected.
                    type: string
                  replaceInvalidCharacters:
                    description: |-
                      8 characters with the Unicode replacement character (�) in query results for an Iceberg table. For more information, check REPLACE_INVALID_CHARACTERS docs.
                      Specifies whether to replace invalid UTF-8 characters with the Unicode replacement character (`�`) in query results for an Iceberg table. For more information, check [REPLACE_INVALID_CHARACTERS docs](https://docs.snowflake.com/en/sql-reference/parameters#replace-invalid-characters).
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.metadataFilePath is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.metadataFilePath)
                || (has(self.initProvider) && has(self.initProvider.metadataFilePath))'
          status:
            description: IcebergTableFromFilesStatus defines the observed state of
              IcebergTableFromFiles.
            properties:
              atProvider:
                properties:
                  catalog:
                    description: |-
                      level default is used.
                      Specifies the identifier for the catalog integration to use for the Iceberg table. If not specified, the account-level default is used.
                    type: string
                  comment:
                    description: |-
                      (String) Specifies a comment for the Iceberg table.
                      Specifies a comment for the Iceberg table.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the Iceberg table. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the Iceberg table. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  describeOutput:
                    description: |-
                      (List of Object) Outputs the result of DESCRIBE ICEBERG TABLE for the given Iceberg table. (see below for nested schema)
                      Outputs the result of `DESCRIBE ICEBERG TABLE` for the given Iceberg table.
                    items:
                      properties:
                        check:
                          description: (String)
                          type: string
                        comment:
                          description: (String) Specifies a comment for the Iceberg
                            table.
                          type: string
                        default:
                          description: (String)
                          type: string
                        expression:
                          description: (String)
                          type: string
                        isNullable:
                          description: (Boolean)
                          type: boolean
                        kind:
                          description: (String)
                          type: string
                        name:
                          description: '(String) Specifies the identifier for the
                            Iceberg table; must be unique for the schema in which
                            the Iceberg table is created. Due to technical limitations
                            (read more here), avoid using the following characters:
                            |, ., ".'
                          type: string
                        nameMapping:
                          description: (String)
                          type: string
                        policyName:
                          description: (String)
                          type: string
                        primaryKey:
                          description: (Boolean)
                          type: boolean
                        privacyDomain:
                          description: (String)
                          type: string
                        sourceIcebergType:
                          description: (String)
                          type: string
                        type:
                          description: (String)
                          type: string
                        uniqueKey:
                          description: (Boolean)
                          type: boolean
                        writeDefault:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  externalVolume:
                    description: |-
                      level default is used.
                      Specifies the identifier for the external volume where the Iceberg table stores its metadata files and data in Parquet format. If not specified, the account-level default is used.
                    type: string
                  fullyQualifiedName:
                    description: |-
                      (String) Fully qualified name of the resource. For more information, see object name resolution.
                      Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  metadataFilePath:
                    description: Specifies the relative path of the Iceberg metadata
                      file in the external volume. Cannot be changed after creation.
                      External changes for this field won't be detected.
                    type: string
                  parameters:
                    description: |-
                      (List of Object) Outputs the result of SHOW PARAMETERS IN ICEBERG TABLE for the given Iceberg table. (see below for nested schema)
                      Outputs the result of `SHOW PARAMETERS IN ICEBERG TABLE` for the given Iceberg table.
                    items:
                      properties:
                        catalog:
                          description: level default is used.
                          items:
                            properties:
                              default:
                                description: (String)
                                type: string
                              description:
                                description: (String)
                                type: string
                              key:
                                description: (String)
                                type: string
                              level:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        externalVolume:
                          description: level default is used.
                          items:
                            properties:
                              default:
                                description: (String)
                                type: string
                              description:
                                description: (String)
                                type: string
                              key:
                                description: (String)
                                type: string
                              level:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        replaceInvalidCharacters:
                          description: 8 characters with the Unicode replacement character
                            (�) in query results for an Iceberg table. For more information,
                            check REPLACE_INVALID_CHARACTERS docs.
                          items:
                            properties:
                              default:
                                description: (String)
                                type: string
                              description:
                                description: (String)
                                type: string
                              key:
                                description: (String)
                                type: string
                              level:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  replaceInvalidCharacters:
                    description: |-
                      8 characters with the Unicode replacement character (�) in query results for an Iceberg table. For more information, check REPLACE_INVALID_CHARACTERS docs.
                      Specifies whether to replace invalid UTF-8 characters with the Unicode replacement character (`�`) in query results for an Iceberg table. For more information, check [REPLACE_INVALID_CHARACTERS docs](https://docs.snowflake.com/en/sql-reference/parameters#replace-invalid-characters).
                    type: boolean
                  schema:
                    description: |-
                      (String) The schema in which to create the Iceberg table. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the Iceberg table. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  showOutput:
                    description: |-
                      (List of Object) Outputs the result of SHOW ICEBERG TABLES for the given Iceberg table. Note that this value will be only recomputed whenever values of fields affecting the output change. (see below for nested schema)
                      Outputs the result of `SHOW ICEBERG TABLES` for the given Iceberg table. Note that this value will be only recomputed whenever values of fields affecting the output change.
                    items:
                      properties:
                        autoRefreshStatus:
                          description: (List of Object) (see below for nested schema)
                          items:
                            properties:
                              currentSnapshotId:
                                description: (Number)
                                format: int64
                                type: integer
                              executionState:
                                description: (String)
                                type: string
                              lastSnapshotTime:
                                description: (String)
                                type: string
                              lastUpdatedTime:
                                description: (String)
                                type: string
                              pendingSnapshotCount:
                                description: (Number)
                                format: int64
                                type: integer
                            type: object
                          type: array
                        baseLocation:
                          description: (String)
                          type: string
                        canWriteMetadata:
                          description: (Boolean)
                          type: boolean
                        catalogName:
                          description: (String)
                          type: string
                        catalogNamespace:
                          description: (String)
                          type: string
                        catalogSyncName:
                          description: (String)
                          type: string
                        catalogTableName:
                          description: (String)
                          type: string
                        comment:
                          description: (String) Specifies a comment for the Iceberg
                            table.
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        currentPartitionSpecId:
                          description: (Number)
                          format: int64
                          type: integer
                        databaseName:
                          description: (String)
                          type: string
                        externalVolumeName:
                          description: (String)
                          type: string
                        icebergTableFormatVersion:
                          description: (Number)
                          format: int64
                          type: integer
                        icebergTableType:
                          description: (String)
                          type: string
                        name:
                          description: '(String) Specifies the identifier for the
                            Iceberg table; must be unique for the schema in which
                            the Iceberg table is created. Due to technical limitations
                            (read more here), avoid using the following characters:
                            |, ., ".'
                          type: string
                        nameMapping:
                          description: (String)
                          type: string
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        partitionSpecs:
                          description: (List of Object) (see below for nested schema)
                          items:
                            properties:
                              fields:
                                description: (List of Object) (see below for nested
                                  schema)
                                items:
                                  properties:
                                    fieldId:
                                      description: (Number)
                                      format: int64
                                      type: integer
                                    name:
                                      description: '(String) Specifies the identifier
                                        for the Iceberg table; must be unique for
                                        the schema in which the Iceberg table is created.
                                        Due to technical limitations (read more here),
                                        avoid using the following characters: |, .,
                                        ".'
                                      type: string
                                    sourceId:
                                      description: (Number)
                                      format: int64
                                      type: integer
                                    transform:
                                      description: (String)
                                      type: string
                                  type: object
                                type: array
                              specId:
                                description: (Number)
                                format: int64
                                type: integer
                            type: object
                          type: array
                        schemaName:
                          description: (String)
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
