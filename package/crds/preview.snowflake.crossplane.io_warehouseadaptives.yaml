---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: warehouseadaptives.preview.snowflake.crossplane.io
spec:
  group: preview.snowflake.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - snowflake
    kind: WarehouseAdaptive
    listKind: WarehouseAdaptiveList
    plural: warehouseadaptives
    singular: warehouseadaptive
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WarehouseAdaptive is the Schema for the WarehouseAdaptives API.
          Resource used to manage adaptive warehouse objects. Adaptive Compute is
          a compute service focused on delivering strong performance with effortless
          operations. It replaces the fixed compute of the Standard Warehouse with
          a workload-aware one that adapts to your queries automatically. The system
          decides how to allocate resources for the best performance, eliminating
          the need for infrastructure tuning.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WarehouseAdaptiveSpec defines the desired state of WarehouseAdaptive
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  comment:
                    description: |-
                      (String) Specifies a comment for the adaptive warehouse.
                      Specifies a comment for the adaptive warehouse.
                    type: string
                  maxQueryPerformanceLevel:
                    description: |-
                      insensitive): XSMALL | SMALL | MEDIUM | LARGE | XLARGE | XXLARGE | XXXLARGE | X4LARGE.
                      Specifies the maximum query performance level for the adaptive warehouse. Determines the initial compute capacity. Valid values are (case-insensitive): `XSMALL` | `SMALL` | `MEDIUM` | `LARGE` | `XLARGE` | `XXLARGE` | `XXXLARGE` | `X4LARGE`.
                    type: string
                  queryThroughputMultiplier:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the query throughput multiplier for the adaptive warehouse.
                      Specifies the query throughput multiplier for the adaptive warehouse.
                    format: int64
                    type: integer
                  resourceMonitor:
                    description: |-
                      (String)
                      Specifies the name of a resource monitor that is explicitly assigned to the adaptive warehouse. For more information about this resource, see [docs](./resource_monitor).
                    type: string
                  statementQueuedTimeoutInSeconds:
                    description: |-
                      (Number) Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                      Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                    format: int64
                    type: integer
                  statementTimeoutInSeconds:
                    description: |-
                      (Number) Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                      Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                    format: int64
                    type: integer
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  comment:
                    description: |-
                      (String) Specifies a comment for the adaptive warehouse.
                      Specifies a comment for the adaptive warehouse.
                    type: string
                  maxQueryPerformanceLevel:
                    description: |-
                      insensitive): XSMALL | SMALL | MEDIUM | LARGE | XLARGE | XXLARGE | XXXLARGE | X4LARGE.
                      Specifies the maximum query performance level for the adaptive warehouse. Determines the initial compute capacity. Valid values are (case-insensitive): `XSMALL` | `SMALL` | `MEDIUM` | `LARGE` | `XLARGE` | `XXLARGE` | `XXXLARGE` | `X4LARGE`.
                    type: string
                  queryThroughputMultiplier:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the query throughput multiplier for the adaptive warehouse.
                      Specifies the query throughput multiplier for the adaptive warehouse.
                    format: int64
                    type: integer
                  resourceMonitor:
                    description: |-
                      (String)
                      Specifies the name of a resource monitor that is explicitly assigned to the adaptive warehouse. For more information about this resource, see [docs](./resource_monitor).
                    type: string
                  statementQueuedTimeoutInSeconds:
                    description: |-
                      (Number) Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                      Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                    format: int64
                    type: integer
                  statementTimeoutInSeconds:
                    description: |-
                      (Number) Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                      Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                    format: int64
                    type: integer
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: WarehouseAdaptiveStatus defines the observed state of WarehouseAdaptive.
            properties:
              atProvider:
                properties:
                  comment:
                    description: |-
                      (String) Specifies a comment for the adaptive warehouse.
                      Specifies a comment for the adaptive warehouse.
                    type: string
                  fullyQualifiedName:
                    description: |-
                      (String) Fully qualified name of the resource. For more information, see object name resolution.
                      Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  maxQueryPerformanceLevel:
                    description: |-
                      insensitive): XSMALL | SMALL | MEDIUM | LARGE | XLARGE | XXLARGE | XXXLARGE | X4LARGE.
                      Specifies the maximum query performance level for the adaptive warehouse. Determines the initial compute capacity. Valid values are (case-insensitive): `XSMALL` | `SMALL` | `MEDIUM` | `LARGE` | `XLARGE` | `XXLARGE` | `XXXLARGE` | `X4LARGE`.
                    type: string
                  parameters:
                    description: |-
                      (List of Object) Outputs the result of SHOW PARAMETERS IN WAREHOUSE for the given adaptive warehouse. (see below for nested schema)
                      Outputs the result of `SHOW PARAMETERS IN WAREHOUSE` for the given adaptive warehouse.
                    items:
                      properties:
                        statementQueuedTimeoutInSeconds:
                          description: (Number) Object parameter that specifies the
                            time, in seconds, a SQL statement (query, DDL, DML, etc.)
                            can be queued on a warehouse before it is canceled by
                            the system.
                          items:
                            properties:
                              default:
                                description: (String)
                                type: string
                              description:
                                description: (String)
                                type: string
                              key:
                                description: (String)
                                type: string
                              level:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                        statementTimeoutInSeconds:
                          description: (Number) Specifies the time, in seconds, after
                            which a running SQL statement (query, DDL, DML, etc.)
                            is canceled by the system.
                          items:
                            properties:
                              default:
                                description: (String)
                                type: string
                              description:
                                description: (String)
                                type: string
                              key:
                                description: (String)
                                type: string
                              level:
                                description: (String)
                                type: string
                              value:
                                description: (String)
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  queryThroughputMultiplier:
                    description: |-
                      uses special value that cannot be set in the configuration manually (-1)) Specifies the query throughput multiplier for the adaptive warehouse.
                      Specifies the query throughput multiplier for the adaptive warehouse.
                    format: int64
                    type: integer
                  resourceMonitor:
                    description: |-
                      (String)
                      Specifies the name of a resource monitor that is explicitly assigned to the adaptive warehouse. For more information about this resource, see [docs](./resource_monitor).
                    type: string
                  showOutput:
                    description: |-
                      (List of Object) Outputs the result of SHOW WAREHOUSES for the given adaptive warehouse. (see below for nested schema)
                      Outputs the result of `SHOW WAREHOUSES` for the given adaptive warehouse.
                    items:
                      properties:
                        autoResume:
                          description: (Boolean)
                          type: boolean
                        available:
                          description: (Number)
                          type: number
                        comment:
                          description: (String) Specifies a comment for the adaptive
                            warehouse.
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        isCurrent:
                          description: (Boolean)
                          type: boolean
                        isDefault:
                          description: (Boolean)
                          type: boolean
                        maxQueryPerformanceLevel:
                          description: 'insensitive): XSMALL | SMALL | MEDIUM | LARGE
                            | XLARGE | XXLARGE | XXXLARGE | X4LARGE.'
                          type: string
                        name:
                          description: '(String) Identifier for the adaptive warehouse;
                            must be unique for your account. Due to technical limitations
                            (read more here), avoid using the following characters:
                            |, ., ".'
                          type: string
                        other:
                          description: (Number)
                          type: number
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        provisioning:
                          description: (Number)
                          type: number
                        queryThroughputMultiplier:
                          description: uses special value that cannot be set in the
                            configuration manually (-1)) Specifies the query throughput
                            multiplier for the adaptive warehouse.
                          format: int64
                          type: integer
                        queued:
                          description: (Number)
                          format: int64
                          type: integer
                        quiescing:
                          description: (Number)
                          type: number
                        resourceMonitor:
                          description: (String)
                          type: string
                        resumedOn:
                          description: (String)
                          type: string
                        running:
                          description: (Number)
                          format: int64
                          type: integer
                        state:
                          description: (String)
                          type: string
                        type:
                          description: (String)
                          type: string
                        updatedOn:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  statementQueuedTimeoutInSeconds:
                    description: |-
                      (Number) Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                      Object parameter that specifies the time, in seconds, a SQL statement (query, DDL, DML, etc.) can be queued on a warehouse before it is canceled by the system.
                    format: int64
                    type: integer
                  statementTimeoutInSeconds:
                    description: |-
                      (Number) Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                      Specifies the time, in seconds, after which a running SQL statement (query, DDL, DML, etc.) is canceled by the system.
                    format: int64
                    type: integer
                  warehouseType:
                    description: |-
                      (String) Specifies the type for the adaptive warehouse. This field is used for checking external changes and recreating the resource if needed.
                      Specifies the type for the adaptive warehouse. This field is used for checking external changes and recreating the resource if needed.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
