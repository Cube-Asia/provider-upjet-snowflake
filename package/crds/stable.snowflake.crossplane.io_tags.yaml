---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: tags.stable.snowflake.crossplane.io
spec:
  group: stable.snowflake.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - snowflake
    kind: Tag
    listKind: TagList
    plural: tags
    singular: tag
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tag is the Schema for the Tags API. Resource used to manage tags.
          For more information, check tag documentation https://docs.snowflake.com/en/sql-reference/sql/create-tag.
          For assigning tags to Snowflake objects, see tag_association resource ./tag_association.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TagSpec defines the desired state of Tag
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  allowedValues:
                    description: |-
                      (Set of String, Deprecated) Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with no_allowed_values and ordered_allowed_values.
                      Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with `no_allowed_values` and `ordered_allowed_values`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the tag.
                      Specifies a comment for the tag.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the tag. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the tag. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  maskingPolicies:
                    description: |-
                      (Set of String) Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see docs.
                      Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see [docs](./masking_policy).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  noAllowedValues:
                    description: |-
                      (Boolean) When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting allowed_values, which means any value is accepted. Available only when the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled. Conflicts with allowed_values and ordered_allowed_values.
                      When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting `allowed_values`, which means any value is accepted. Available only when the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled. Conflicts with `allowed_values` and `ordered_allowed_values`.
                    type: boolean
                  onConflict:
                    description: |-
                      (Block List, Max: 1) Specifies what happens when there is a conflict between the values of propagated tags. (see below for nested schema)
                      Specifies what happens when there is a conflict between the values of [propagated tags](https://docs.snowflake.com/en/user-guide/object-tagging/propagation).
                    items:
                      properties:
                        allowedValuesSequence:
                          description: |-
                            (Boolean) The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                            The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                          type: boolean
                        customValue:
                          description: |-
                            (String) Whenever there is a conflict, the value of tag is set to custom_value. If allowed_values are set, the value set in this field should be one of the values in the allowed_values list.
                            Whenever there is a conflict, the value of tag is set to custom_value. If `allowed_values` are set, the value set in this field should be one of the values in the `allowed_values` list.
                          type: string
                      type: object
                    type: array
                  orderedAllowedValues:
                    description: |-
                      in-place for this field, even when the configured order already matches the order stored in Snowflake.
                      Ordered list of allowed values for the tag. The order is preserved in Snowflake and is significant when `on_conflict.allowed_values_sequence` is used — the first matching value in the sequence wins. Use this instead of `allowed_values` when order matters. Conflicts with `allowed_values` and `no_allowed_values`. Note: transitioning from `allowed_values` to `ordered_allowed_values` always plans an update-in-place for this field, even when the configured order already matches the order stored in Snowflake.
                    items:
                      type: string
                    type: array
                  propagate:
                    description: |-
                      (String) Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the official documentation. Valid options are: NONE | ON_DEPENDENCY | ON_DATA_MOVEMENT | ON_DEPENDENCY_AND_DATA_MOVEMENT
                      Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the [official documentation](https://docs.snowflake.com/en/user-guide/object-tagging/propagation). Valid options are: `NONE` | `ON_DEPENDENCY` | `ON_DATA_MOVEMENT` | `ON_DEPENDENCY_AND_DATA_MOVEMENT`
                    type: string
                  schema:
                    description: |-
                      (String) The schema in which to create the tag. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the tag. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                required:
                - database
                - schema
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  allowedValues:
                    description: |-
                      (Set of String, Deprecated) Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with no_allowed_values and ordered_allowed_values.
                      Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with `no_allowed_values` and `ordered_allowed_values`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the tag.
                      Specifies a comment for the tag.
                    type: string
                  maskingPolicies:
                    description: |-
                      (Set of String) Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see docs.
                      Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see [docs](./masking_policy).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  noAllowedValues:
                    description: |-
                      (Boolean) When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting allowed_values, which means any value is accepted. Available only when the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled. Conflicts with allowed_values and ordered_allowed_values.
                      When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting `allowed_values`, which means any value is accepted. Available only when the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled. Conflicts with `allowed_values` and `ordered_allowed_values`.
                    type: boolean
                  onConflict:
                    description: |-
                      (Block List, Max: 1) Specifies what happens when there is a conflict between the values of propagated tags. (see below for nested schema)
                      Specifies what happens when there is a conflict between the values of [propagated tags](https://docs.snowflake.com/en/user-guide/object-tagging/propagation).
                    items:
                      properties:
                        allowedValuesSequence:
                          description: |-
                            (Boolean) The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                            The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                          type: boolean
                        customValue:
                          description: |-
                            (String) Whenever there is a conflict, the value of tag is set to custom_value. If allowed_values are set, the value set in this field should be one of the values in the allowed_values list.
                            Whenever there is a conflict, the value of tag is set to custom_value. If `allowed_values` are set, the value set in this field should be one of the values in the `allowed_values` list.
                          type: string
                      type: object
                    type: array
                  orderedAllowedValues:
                    description: |-
                      in-place for this field, even when the configured order already matches the order stored in Snowflake.
                      Ordered list of allowed values for the tag. The order is preserved in Snowflake and is significant when `on_conflict.allowed_values_sequence` is used — the first matching value in the sequence wins. Use this instead of `allowed_values` when order matters. Conflicts with `allowed_values` and `no_allowed_values`. Note: transitioning from `allowed_values` to `ordered_allowed_values` always plans an update-in-place for this field, even when the configured order already matches the order stored in Snowflake.
                    items:
                      type: string
                    type: array
                  propagate:
                    description: |-
                      (String) Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the official documentation. Valid options are: NONE | ON_DEPENDENCY | ON_DATA_MOVEMENT | ON_DEPENDENCY_AND_DATA_MOVEMENT
                      Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the [official documentation](https://docs.snowflake.com/en/user-guide/object-tagging/propagation). Valid options are: `NONE` | `ON_DEPENDENCY` | `ON_DATA_MOVEMENT` | `ON_DEPENDENCY_AND_DATA_MOVEMENT`
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: TagStatus defines the observed state of Tag.
            properties:
              atProvider:
                properties:
                  allowedValues:
                    description: |-
                      (Set of String, Deprecated) Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with no_allowed_values and ordered_allowed_values.
                      Set of allowed values for the tag (unordered). When specified, only these values can be assigned. When the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled, removing this field from the configuration reverts the tag to accepting any value. Conflicts with `no_allowed_values` and `ordered_allowed_values`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the tag.
                      Specifies a comment for the tag.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the tag. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the tag. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  fullyQualifiedName:
                    description: |-
                      (String) Fully qualified name of the resource. For more information, see object name resolution.
                      Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  maskingPolicies:
                    description: |-
                      (Set of String) Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see docs.
                      Set of masking policies for the tag. A tag can support one masking policy for each data type. If masking policies are assigned to the tag, before dropping the tag, the provider automatically unassigns them. For more information about this resource, see [docs](./masking_policy).
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  noAllowedValues:
                    description: |-
                      (Boolean) When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting allowed_values, which means any value is accepted. Available only when the TAGS_ALLOW_EMPTY_ALLOWED_VALUES experiment is enabled. Conflicts with allowed_values and ordered_allowed_values.
                      When set to true, the tag explicitly disallows any value from being assigned. This is different from omitting `allowed_values`, which means any value is accepted. Available only when the `TAGS_ALLOW_EMPTY_ALLOWED_VALUES` experiment is enabled. Conflicts with `allowed_values` and `ordered_allowed_values`.
                    type: boolean
                  onConflict:
                    description: |-
                      (Block List, Max: 1) Specifies what happens when there is a conflict between the values of propagated tags. (see below for nested schema)
                      Specifies what happens when there is a conflict between the values of [propagated tags](https://docs.snowflake.com/en/user-guide/object-tagging/propagation).
                    items:
                      properties:
                        allowedValuesSequence:
                          description: |-
                            (Boolean) The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                            The order of the values in the ALLOWED_VALUES property of the tag determines which value is used when there is a conflict.
                          type: boolean
                        customValue:
                          description: |-
                            (String) Whenever there is a conflict, the value of tag is set to custom_value. If allowed_values are set, the value set in this field should be one of the values in the allowed_values list.
                            Whenever there is a conflict, the value of tag is set to custom_value. If `allowed_values` are set, the value set in this field should be one of the values in the `allowed_values` list.
                          type: string
                      type: object
                    type: array
                  orderedAllowedValues:
                    description: |-
                      in-place for this field, even when the configured order already matches the order stored in Snowflake.
                      Ordered list of allowed values for the tag. The order is preserved in Snowflake and is significant when `on_conflict.allowed_values_sequence` is used — the first matching value in the sequence wins. Use this instead of `allowed_values` when order matters. Conflicts with `allowed_values` and `no_allowed_values`. Note: transitioning from `allowed_values` to `ordered_allowed_values` always plans an update-in-place for this field, even when the configured order already matches the order stored in Snowflake.
                    items:
                      type: string
                    type: array
                  propagate:
                    description: |-
                      (String) Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the official documentation. Valid options are: NONE | ON_DEPENDENCY | ON_DATA_MOVEMENT | ON_DEPENDENCY_AND_DATA_MOVEMENT
                      Specifies that the tag will be automatically propagated from source objects to target objects. See more about tag propagation in the [official documentation](https://docs.snowflake.com/en/user-guide/object-tagging/propagation). Valid options are: `NONE` | `ON_DEPENDENCY` | `ON_DATA_MOVEMENT` | `ON_DEPENDENCY_AND_DATA_MOVEMENT`
                    type: string
                  schema:
                    description: |-
                      (String) The schema in which to create the tag. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the tag. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  showOutput:
                    description: |-
                      (List of Object) Outputs the result of SHOW TAGS for the given tag. (see below for nested schema)
                      Outputs the result of `SHOW TAGS` for the given tag.
                    items:
                      properties:
                        allowedValues:
                          description: (Set of String, Deprecated) Set of allowed
                            values for the tag (unordered). When specified, only these
                            values can be assigned. When the TAGS_ALLOW_EMPTY_ALLOWED_VALUES
                            experiment is enabled, removing this field from the configuration
                            reverts the tag to accepting any value. Conflicts with
                            no_allowed_values and ordered_allowed_values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        comment:
                          description: (String) Specifies a comment for the tag.
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        databaseName:
                          description: (String)
                          type: string
                        name:
                          description: '(String) Specifies the identifier for the
                            tag; must be unique for the database in which the tag
                            is created. Due to technical limitations (read more here),
                            avoid using the following characters: |, ., ".'
                          type: string
                        onConflict:
                          description: '(Block List, Max: 1) Specifies what happens
                            when there is a conflict between the values of propagated
                            tags. (see below for nested schema)'
                          type: string
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        propagate:
                          description: '(String) Specifies that the tag will be automatically
                            propagated from source objects to target objects. See
                            more about tag propagation in the official documentation.
                            Valid options are: NONE | ON_DEPENDENCY | ON_DATA_MOVEMENT
                            | ON_DEPENDENCY_AND_DATA_MOVEMENT'
                          type: string
                        schemaName:
                          description: (String)
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
