---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: authenticationpolicies.stable.snowflake.m.crossplane.io
spec:
  group: stable.snowflake.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - snowflake
    kind: AuthenticationPolicy
    listKind: AuthenticationPolicyList
    plural: authenticationpolicies
    singular: authenticationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AuthenticationPolicy is the Schema for the AuthenticationPolicys
          API. Resource used to manage authentication policy objects. For more information,
          check authentication policy documentation https://docs.snowflake.com/en/sql-reference/sql/create-authentication-policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AuthenticationPolicySpec defines the desired state of AuthenticationPolicy
            properties:
              forProvider:
                properties:
                  authenticationMethods:
                    description: |-
                      insensitive): ALL | SAML | PASSWORD | OAUTH | KEYPAIR | PROGRAMMATIC_ACCESS_TOKEN | WORKLOAD_IDENTITY.
                      A list of authentication methods that are allowed during login. Valid values are (case-insensitive): `ALL` | `SAML` | `PASSWORD` | `OAUTH` | `KEYPAIR` | `PROGRAMMATIC_ACCESS_TOKEN` | `WORKLOAD_IDENTITY`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clientPolicy:
                    description: |-
                      client type. Only valid when client_types is empty, contains ALL, or contains DRIVERS. (see below for nested schema)
                      Allows to set policies per-client type. Only valid when `client_types` is empty, contains ALL, or contains DRIVERS.
                    items:
                      properties:
                        clientType:
                          description: |-
                            insensitive): JDBC_DRIVER | ODBC_DRIVER | PYTHON_DRIVER | JAVASCRIPT_DRIVER | C_DRIVER | GO_DRIVER | PHP_DRIVER | DOTNET_DRIVER | SQL_API | SNOWPIPE_STREAMING_CLIENT_SDK | PY_CORE | SPROC_PYTHON | PYTHON_SNOWPARK | SQL_ALCHEMY | SNOWPARK | SNOWFLAKE_CLIENT.
                            The client or driver type. Valid values (case-insensitive): `JDBC_DRIVER` | `ODBC_DRIVER` | `PYTHON_DRIVER` | `JAVASCRIPT_DRIVER` | `C_DRIVER` | `GO_DRIVER` | `PHP_DRIVER` | `DOTNET_DRIVER` | `SQL_API` | `SNOWPIPE_STREAMING_CLIENT_SDK` | `PY_CORE` | `SPROC_PYTHON` | `PYTHON_SNOWPARK` | `SQL_ALCHEMY` | `SNOWPARK` | `SNOWFLAKE_CLIENT`.
                          type: string
                        minimumVersion:
                          description: |-
                            (String) Minimum allowed version for this client/driver type (e.g. '1.14.1').
                            Minimum allowed version for this client/driver type (e.g. '1.14.1').
                          type: string
                      type: object
                    type: array
                  clientTypes:
                    description: |-
                      insensitive): ALL | SNOWFLAKE_UI | DRIVERS | SNOWSQL | SNOWFLAKE_CLI. The client_types property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                      A list of clients that can authenticate with Snowflake. If a client tries to connect, and the client is not one of the valid `client_types`, then the login attempt fails. Valid values are (case-insensitive): `ALL` | `SNOWFLAKE_UI` | `DRIVERS` | `SNOWSQL` | `SNOWFLAKE_CLI`. The `client_types` property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the authentication policy.
                      Specifies a comment for the authentication policy.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the authentication policy. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the authentication policy. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  mfaAuthenticationMethods:
                    description: |-
                      factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are ALL | SAML | PASSWORD.
                      A list of authentication methods that enforce multi-factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are `ALL` | `SAML` | `PASSWORD`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  mfaEnrollment:
                    description: |-
                      factor authentication. Valid values are (case-insensitive): REQUIRED | REQUIRED_PASSWORD_ONLY | OPTIONAL. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the client_types parameter must include snowflake_ui, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to REQUIRED_PASSWORD_ONLY, which may cause permadiff. In this case, you may want to adjust this field value.
                      Determines whether a user must enroll in multi-factor authentication. Valid values are (case-insensitive): `REQUIRED` | `REQUIRED_PASSWORD_ONLY` | `OPTIONAL`. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the `client_types` parameter must include `snowflake_ui`, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to `REQUIRED_PASSWORD_ONLY`, which may cause permadiff. In this case, you may want to adjust this field value.
                    type: string
                  mfaPolicy:
                    description: |-
                      factor authentication (MFA) methods that users can use as a second factor of authentication. (see below for nested schema)
                      Specifies the multi-factor authentication (MFA) methods that users can use as a second factor of authentication.
                    items:
                      properties:
                        allowedMethods:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed methods for the MFA policy. Valid values are: `ALL` | `PASSKEY` | `TOTP` | `OTP` | `DUO`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        enforceMfaOnExternalAuthentication:
                          description: |-
                            factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): ALL | NONE.
                            Determines whether multi-factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): `ALL` | `NONE`.
                          type: string
                      type: object
                    type: array
                  patPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for programmatic access tokens. (see below for nested schema)
                      Specifies the policy for programmatic access tokens.
                    items:
                      properties:
                        defaultExpiryInDays:
                          description: |-
                            (Number) Specifies the default expiration time (in days) for a programmatic access token.
                            Specifies the default expiration time (in days) for a programmatic access token.
                          format: int64
                          type: integer
                        maxExpiryInDays:
                          description: |-
                            (Number) Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                            Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                          format: int64
                          type: integer
                        networkPolicyEvaluation:
                          description: |-
                            (String) Specifies the network policy evaluation for the PAT. Valid values are: ENFORCED_REQUIRED | ENFORCED_NOT_REQUIRED | NOT_ENFORCED.
                            Specifies the network policy evaluation for the PAT. Valid values are: `ENFORCED_REQUIRED` | `ENFORCED_NOT_REQUIRED` | `NOT_ENFORCED`.
                          type: string
                        requireRoleRestrictionForServiceUsers:
                          description: |-
                            uses special value that cannot be set in the configuration manually (default)) If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                            If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                          type: string
                      type: object
                    type: array
                  schema:
                    description: |-
                      (String) The schema in which to create the authentication policy. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the authentication policy. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  securityIntegrations:
                    description: |-
                      (Set of String) A list of security integrations the authentication policy is associated with. This parameter has no effect when saml or oauth are not in the authentication_methods list. All values in the security_integrations list must be compatible with the values in the authentication_methods list. For example, if security_integrations contains a SAML security integration, and authentication_methods contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use ALL as parameter.
                      A list of security integrations the authentication policy is associated with. This parameter has no effect when `saml` or `oauth` are not in the `authentication_methods` list. All values in the `security_integrations` list must be compatible with the values in the `authentication_methods` list. For example, if `security_integrations` contains a SAML security integration, and `authentication_methods` contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use `ALL` as parameter.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  workloadIdentityPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for workload identity federation. (see below for nested schema)
                      Specifies the policy for workload identity federation.
                    items:
                      properties:
                        allowedAwsAccounts:
                          description: |-
                            (Set of String) Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type AWS.
                            Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type `AWS`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedAzureIssuers:
                          description: |-
                            (Set of String) Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type AZURE.
                            Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type `AZURE`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOidcIssuers:
                          description: |-
                            (Set of String) Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type OIDC.
                            Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type `OIDC`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedProviders:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed providers for the workload identity policy. Valid values are: `ALL` | `AWS` | `AZURE` | `GCP` | `OIDC`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                required:
                - database
                - schema
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationMethods:
                    description: |-
                      insensitive): ALL | SAML | PASSWORD | OAUTH | KEYPAIR | PROGRAMMATIC_ACCESS_TOKEN | WORKLOAD_IDENTITY.
                      A list of authentication methods that are allowed during login. Valid values are (case-insensitive): `ALL` | `SAML` | `PASSWORD` | `OAUTH` | `KEYPAIR` | `PROGRAMMATIC_ACCESS_TOKEN` | `WORKLOAD_IDENTITY`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clientPolicy:
                    description: |-
                      client type. Only valid when client_types is empty, contains ALL, or contains DRIVERS. (see below for nested schema)
                      Allows to set policies per-client type. Only valid when `client_types` is empty, contains ALL, or contains DRIVERS.
                    items:
                      properties:
                        clientType:
                          description: |-
                            insensitive): JDBC_DRIVER | ODBC_DRIVER | PYTHON_DRIVER | JAVASCRIPT_DRIVER | C_DRIVER | GO_DRIVER | PHP_DRIVER | DOTNET_DRIVER | SQL_API | SNOWPIPE_STREAMING_CLIENT_SDK | PY_CORE | SPROC_PYTHON | PYTHON_SNOWPARK | SQL_ALCHEMY | SNOWPARK | SNOWFLAKE_CLIENT.
                            The client or driver type. Valid values (case-insensitive): `JDBC_DRIVER` | `ODBC_DRIVER` | `PYTHON_DRIVER` | `JAVASCRIPT_DRIVER` | `C_DRIVER` | `GO_DRIVER` | `PHP_DRIVER` | `DOTNET_DRIVER` | `SQL_API` | `SNOWPIPE_STREAMING_CLIENT_SDK` | `PY_CORE` | `SPROC_PYTHON` | `PYTHON_SNOWPARK` | `SQL_ALCHEMY` | `SNOWPARK` | `SNOWFLAKE_CLIENT`.
                          type: string
                        minimumVersion:
                          description: |-
                            (String) Minimum allowed version for this client/driver type (e.g. '1.14.1').
                            Minimum allowed version for this client/driver type (e.g. '1.14.1').
                          type: string
                      type: object
                    type: array
                  clientTypes:
                    description: |-
                      insensitive): ALL | SNOWFLAKE_UI | DRIVERS | SNOWSQL | SNOWFLAKE_CLI. The client_types property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                      A list of clients that can authenticate with Snowflake. If a client tries to connect, and the client is not one of the valid `client_types`, then the login attempt fails. Valid values are (case-insensitive): `ALL` | `SNOWFLAKE_UI` | `DRIVERS` | `SNOWSQL` | `SNOWFLAKE_CLI`. The `client_types` property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the authentication policy.
                      Specifies a comment for the authentication policy.
                    type: string
                  mfaAuthenticationMethods:
                    description: |-
                      factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are ALL | SAML | PASSWORD.
                      A list of authentication methods that enforce multi-factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are `ALL` | `SAML` | `PASSWORD`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  mfaEnrollment:
                    description: |-
                      factor authentication. Valid values are (case-insensitive): REQUIRED | REQUIRED_PASSWORD_ONLY | OPTIONAL. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the client_types parameter must include snowflake_ui, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to REQUIRED_PASSWORD_ONLY, which may cause permadiff. In this case, you may want to adjust this field value.
                      Determines whether a user must enroll in multi-factor authentication. Valid values are (case-insensitive): `REQUIRED` | `REQUIRED_PASSWORD_ONLY` | `OPTIONAL`. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the `client_types` parameter must include `snowflake_ui`, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to `REQUIRED_PASSWORD_ONLY`, which may cause permadiff. In this case, you may want to adjust this field value.
                    type: string
                  mfaPolicy:
                    description: |-
                      factor authentication (MFA) methods that users can use as a second factor of authentication. (see below for nested schema)
                      Specifies the multi-factor authentication (MFA) methods that users can use as a second factor of authentication.
                    items:
                      properties:
                        allowedMethods:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed methods for the MFA policy. Valid values are: `ALL` | `PASSKEY` | `TOTP` | `OTP` | `DUO`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        enforceMfaOnExternalAuthentication:
                          description: |-
                            factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): ALL | NONE.
                            Determines whether multi-factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): `ALL` | `NONE`.
                          type: string
                      type: object
                    type: array
                  patPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for programmatic access tokens. (see below for nested schema)
                      Specifies the policy for programmatic access tokens.
                    items:
                      properties:
                        defaultExpiryInDays:
                          description: |-
                            (Number) Specifies the default expiration time (in days) for a programmatic access token.
                            Specifies the default expiration time (in days) for a programmatic access token.
                          format: int64
                          type: integer
                        maxExpiryInDays:
                          description: |-
                            (Number) Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                            Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                          format: int64
                          type: integer
                        networkPolicyEvaluation:
                          description: |-
                            (String) Specifies the network policy evaluation for the PAT. Valid values are: ENFORCED_REQUIRED | ENFORCED_NOT_REQUIRED | NOT_ENFORCED.
                            Specifies the network policy evaluation for the PAT. Valid values are: `ENFORCED_REQUIRED` | `ENFORCED_NOT_REQUIRED` | `NOT_ENFORCED`.
                          type: string
                        requireRoleRestrictionForServiceUsers:
                          description: |-
                            uses special value that cannot be set in the configuration manually (default)) If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                            If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                          type: string
                      type: object
                    type: array
                  securityIntegrations:
                    description: |-
                      (Set of String) A list of security integrations the authentication policy is associated with. This parameter has no effect when saml or oauth are not in the authentication_methods list. All values in the security_integrations list must be compatible with the values in the authentication_methods list. For example, if security_integrations contains a SAML security integration, and authentication_methods contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use ALL as parameter.
                      A list of security integrations the authentication policy is associated with. This parameter has no effect when `saml` or `oauth` are not in the `authentication_methods` list. All values in the `security_integrations` list must be compatible with the values in the `authentication_methods` list. For example, if `security_integrations` contains a SAML security integration, and `authentication_methods` contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use `ALL` as parameter.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  workloadIdentityPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for workload identity federation. (see below for nested schema)
                      Specifies the policy for workload identity federation.
                    items:
                      properties:
                        allowedAwsAccounts:
                          description: |-
                            (Set of String) Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type AWS.
                            Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type `AWS`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedAzureIssuers:
                          description: |-
                            (Set of String) Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type AZURE.
                            Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type `AZURE`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOidcIssuers:
                          description: |-
                            (Set of String) Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type OIDC.
                            Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type `OIDC`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedProviders:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed providers for the workload identity policy. Valid values are: `ALL` | `AWS` | `AZURE` | `GCP` | `OIDC`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: AuthenticationPolicyStatus defines the observed state of
              AuthenticationPolicy.
            properties:
              atProvider:
                properties:
                  authenticationMethods:
                    description: |-
                      insensitive): ALL | SAML | PASSWORD | OAUTH | KEYPAIR | PROGRAMMATIC_ACCESS_TOKEN | WORKLOAD_IDENTITY.
                      A list of authentication methods that are allowed during login. Valid values are (case-insensitive): `ALL` | `SAML` | `PASSWORD` | `OAUTH` | `KEYPAIR` | `PROGRAMMATIC_ACCESS_TOKEN` | `WORKLOAD_IDENTITY`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  clientPolicy:
                    description: |-
                      client type. Only valid when client_types is empty, contains ALL, or contains DRIVERS. (see below for nested schema)
                      Allows to set policies per-client type. Only valid when `client_types` is empty, contains ALL, or contains DRIVERS.
                    items:
                      properties:
                        clientType:
                          description: |-
                            insensitive): JDBC_DRIVER | ODBC_DRIVER | PYTHON_DRIVER | JAVASCRIPT_DRIVER | C_DRIVER | GO_DRIVER | PHP_DRIVER | DOTNET_DRIVER | SQL_API | SNOWPIPE_STREAMING_CLIENT_SDK | PY_CORE | SPROC_PYTHON | PYTHON_SNOWPARK | SQL_ALCHEMY | SNOWPARK | SNOWFLAKE_CLIENT.
                            The client or driver type. Valid values (case-insensitive): `JDBC_DRIVER` | `ODBC_DRIVER` | `PYTHON_DRIVER` | `JAVASCRIPT_DRIVER` | `C_DRIVER` | `GO_DRIVER` | `PHP_DRIVER` | `DOTNET_DRIVER` | `SQL_API` | `SNOWPIPE_STREAMING_CLIENT_SDK` | `PY_CORE` | `SPROC_PYTHON` | `PYTHON_SNOWPARK` | `SQL_ALCHEMY` | `SNOWPARK` | `SNOWFLAKE_CLIENT`.
                          type: string
                        minimumVersion:
                          description: |-
                            (String) Minimum allowed version for this client/driver type (e.g. '1.14.1').
                            Minimum allowed version for this client/driver type (e.g. '1.14.1').
                          type: string
                      type: object
                    type: array
                  clientTypes:
                    description: |-
                      insensitive): ALL | SNOWFLAKE_UI | DRIVERS | SNOWSQL | SNOWFLAKE_CLI. The client_types property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                      A list of clients that can authenticate with Snowflake. If a client tries to connect, and the client is not one of the valid `client_types`, then the login attempt fails. Valid values are (case-insensitive): `ALL` | `SNOWFLAKE_UI` | `DRIVERS` | `SNOWSQL` | `SNOWFLAKE_CLI`. The `client_types` property of an authentication policy is a best effort method to block user logins based on specific clients. It should not be used as the sole control to establish a security boundary.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  comment:
                    description: |-
                      (String) Specifies a comment for the authentication policy.
                      Specifies a comment for the authentication policy.
                    type: string
                  database:
                    description: |-
                      (String) The database in which to create the authentication policy. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The database in which to create the authentication policy. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  describeOutput:
                    description: |-
                      (List of Object) Outputs the result of DESCRIBE AUTHENTICATION POLICY for the given policy. (see below for nested schema)
                      Outputs the result of `DESCRIBE AUTHENTICATION POLICY` for the given policy.
                    items:
                      properties:
                        authenticationMethods:
                          description: 'insensitive): ALL | SAML | PASSWORD | OAUTH
                            | KEYPAIR | PROGRAMMATIC_ACCESS_TOKEN | WORKLOAD_IDENTITY.'
                          type: string
                        clientPolicy:
                          description: client type. Only valid when client_types is
                            empty, contains ALL, or contains DRIVERS. (see below for
                            nested schema)
                          type: string
                        clientTypes:
                          description: 'insensitive): ALL | SNOWFLAKE_UI | DRIVERS
                            | SNOWSQL | SNOWFLAKE_CLI. The client_types property of
                            an authentication policy is a best effort method to block
                            user logins based on specific clients. It should not be
                            used as the sole control to establish a security boundary.'
                          type: string
                        comment:
                          description: (String) Specifies a comment for the authentication
                            policy.
                          type: string
                        mfaEnrollment:
                          description: 'factor authentication. Valid values are (case-insensitive):
                            REQUIRED | REQUIRED_PASSWORD_ONLY | OPTIONAL. When REQUIRED
                            is specified, Enforces users to enroll in MFA. If this
                            value is used, then the client_types parameter must include
                            snowflake_ui, because Snowsight is the only place users
                            can enroll in multi-factor authentication (MFA). Note
                            that when you set this value to OPTIONAL, and your account
                            setup forces users to enroll in MFA, then Snowflake may
                            set quietly this value to REQUIRED_PASSWORD_ONLY, which
                            may cause permadiff. In this case, you may want to adjust
                            this field value.'
                          type: string
                        mfaPolicy:
                          description: factor authentication (MFA) methods that users
                            can use as a second factor of authentication. (see below
                            for nested schema)
                          type: string
                        name:
                          description: '(String) Specifies the identifier for the
                            authentication policy. Due to technical limitations (read
                            more here), avoid using the following characters: |, .,
                            ".'
                          type: string
                        owner:
                          description: (String)
                          type: string
                        patPolicy:
                          description: '(Block List, Max: 1) Specifies the policy
                            for programmatic access tokens. (see below for nested
                            schema)'
                          type: string
                        securityIntegrations:
                          description: (Set of String) A list of security integrations
                            the authentication policy is associated with. This parameter
                            has no effect when saml or oauth are not in the authentication_methods
                            list. All values in the security_integrations list must
                            be compatible with the values in the authentication_methods
                            list. For example, if security_integrations contains a
                            SAML security integration, and authentication_methods
                            contains OAUTH, then you cannot create the authentication
                            policy. To allow all security integrations use ALL as
                            parameter.
                          type: string
                        workloadIdentityPolicy:
                          description: '(Block List, Max: 1) Specifies the policy
                            for workload identity federation. (see below for nested
                            schema)'
                          type: string
                      type: object
                    type: array
                  fullyQualifiedName:
                    description: |-
                      (String) Fully qualified name of the resource. For more information, see object name resolution.
                      Fully qualified name of the resource. For more information, see [object name resolution](https://docs.snowflake.com/en/sql-reference/name-resolution).
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  mfaAuthenticationMethods:
                    description: |-
                      factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are ALL | SAML | PASSWORD.
                      A list of authentication methods that enforce multi-factor authentication (MFA) during login. Authentication methods not listed in this parameter do not prompt for multi-factor authentication. Allowed values are `ALL` | `SAML` | `PASSWORD`.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  mfaEnrollment:
                    description: |-
                      factor authentication. Valid values are (case-insensitive): REQUIRED | REQUIRED_PASSWORD_ONLY | OPTIONAL. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the client_types parameter must include snowflake_ui, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to REQUIRED_PASSWORD_ONLY, which may cause permadiff. In this case, you may want to adjust this field value.
                      Determines whether a user must enroll in multi-factor authentication. Valid values are (case-insensitive): `REQUIRED` | `REQUIRED_PASSWORD_ONLY` | `OPTIONAL`. When REQUIRED is specified, Enforces users to enroll in MFA. If this value is used, then the `client_types` parameter must include `snowflake_ui`, because Snowsight is the only place users can enroll in multi-factor authentication (MFA). Note that when you set this value to OPTIONAL, and your account setup forces users to enroll in MFA, then Snowflake may set quietly this value to `REQUIRED_PASSWORD_ONLY`, which may cause permadiff. In this case, you may want to adjust this field value.
                    type: string
                  mfaPolicy:
                    description: |-
                      factor authentication (MFA) methods that users can use as a second factor of authentication. (see below for nested schema)
                      Specifies the multi-factor authentication (MFA) methods that users can use as a second factor of authentication.
                    items:
                      properties:
                        allowedMethods:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed methods for the MFA policy. Valid values are: `ALL` | `PASSKEY` | `TOTP` | `OTP` | `DUO`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        enforceMfaOnExternalAuthentication:
                          description: |-
                            factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): ALL | NONE.
                            Determines whether multi-factor authentication (MFA) is enforced on external authentication. Valid values are (case-insensitive): `ALL` | `NONE`.
                          type: string
                      type: object
                    type: array
                  patPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for programmatic access tokens. (see below for nested schema)
                      Specifies the policy for programmatic access tokens.
                    items:
                      properties:
                        defaultExpiryInDays:
                          description: |-
                            (Number) Specifies the default expiration time (in days) for a programmatic access token.
                            Specifies the default expiration time (in days) for a programmatic access token.
                          format: int64
                          type: integer
                        maxExpiryInDays:
                          description: |-
                            (Number) Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                            Specifies the maximum number of days that can be set for the expiration time for a programmatic access token.
                          format: int64
                          type: integer
                        networkPolicyEvaluation:
                          description: |-
                            (String) Specifies the network policy evaluation for the PAT. Valid values are: ENFORCED_REQUIRED | ENFORCED_NOT_REQUIRED | NOT_ENFORCED.
                            Specifies the network policy evaluation for the PAT. Valid values are: `ENFORCED_REQUIRED` | `ENFORCED_NOT_REQUIRED` | `NOT_ENFORCED`.
                          type: string
                        requireRoleRestrictionForServiceUsers:
                          description: |-
                            uses special value that cannot be set in the configuration manually (default)) If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                            If true, when you generate a programmatic access token for a service user, you must restrict the use of that token to a specific role. Available options are: "true" or "false". When the value is not set in the configuration the provider will put "default" there which means to use the Snowflake default for this value.
                          type: string
                      type: object
                    type: array
                  schema:
                    description: |-
                      (String) The schema in which to create the authentication policy. Due to technical limitations (read more here), avoid using the following characters: |, ., ".
                      The schema in which to create the authentication policy. Due to technical limitations (read more [here](../guides/identifiers_rework_design_decisions#known-limitations-and-identifier-recommendations)), avoid using the following characters: `|`, `.`, `"`.
                    type: string
                  securityIntegrations:
                    description: |-
                      (Set of String) A list of security integrations the authentication policy is associated with. This parameter has no effect when saml or oauth are not in the authentication_methods list. All values in the security_integrations list must be compatible with the values in the authentication_methods list. For example, if security_integrations contains a SAML security integration, and authentication_methods contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use ALL as parameter.
                      A list of security integrations the authentication policy is associated with. This parameter has no effect when `saml` or `oauth` are not in the `authentication_methods` list. All values in the `security_integrations` list must be compatible with the values in the `authentication_methods` list. For example, if `security_integrations` contains a SAML security integration, and `authentication_methods` contains OAUTH, then you cannot create the authentication policy. To allow all security integrations use `ALL` as parameter.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  showOutput:
                    description: |-
                      (List of Object) Outputs the result of SHOW AUTHENTICATION POLICIES for the given policy. (see below for nested schema)
                      Outputs the result of `SHOW AUTHENTICATION POLICIES` for the given policy.
                    items:
                      properties:
                        comment:
                          description: (String) Specifies a comment for the authentication
                            policy.
                          type: string
                        createdOn:
                          description: (String)
                          type: string
                        databaseName:
                          description: (String)
                          type: string
                        kind:
                          description: (String)
                          type: string
                        name:
                          description: '(String) Specifies the identifier for the
                            authentication policy. Due to technical limitations (read
                            more here), avoid using the following characters: |, .,
                            ".'
                          type: string
                        options:
                          description: (String)
                          type: string
                        owner:
                          description: (String)
                          type: string
                        ownerRoleType:
                          description: (String)
                          type: string
                        schemaName:
                          description: (String)
                          type: string
                      type: object
                    type: array
                  workloadIdentityPolicy:
                    description: |-
                      (Block List, Max: 1) Specifies the policy for workload identity federation. (see below for nested schema)
                      Specifies the policy for workload identity federation.
                    items:
                      properties:
                        allowedAwsAccounts:
                          description: |-
                            (Set of String) Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type AWS.
                            Specifies the list of AWS account IDs allowed by the authentication policy during workload identity authentication of type `AWS`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedAzureIssuers:
                          description: |-
                            (Set of String) Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type AZURE.
                            Specifies the list of Azure Entra ID issuers allowed by the authentication policy during workload identity authentication of type `AZURE`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedOidcIssuers:
                          description: |-
                            (Set of String) Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type OIDC.
                            Specifies the list of OIDC issuers allowed by the authentication policy during workload identity authentication of type `OIDC`.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        allowedProviders:
                          description: |-
                            Prefer using uppercased values.
                            Specifies the allowed providers for the workload identity policy. Valid values are: `ALL` | `AWS` | `AZURE` | `GCP` | `OIDC`. Prefer using uppercased values.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
